# Persents real on deposit

def percents(x, y):
     """What percentage of x is y"""
     one_percent = x / 100
     result = y / one_percent
     return result
  #   print("deposit = " + str(x) + " in this moment " + str(y) + " result in procent " + str(result))
 #percents(79,114)


#def set_colour(result):
 #    "\033[0;32;47m Green"
  #   color(result) = 'white'
   #  if result(percents(x, y)) > 100:
    #      color = 'green'
     #else:
      #    color(result) = 'red'
          #if result(percents(x, y)) < 100
    # return color(result)

#print("\033[0;32;47m Green")


def print_percents(x,y):
     """Print What percentage of x is y"""
     print("deposit = " +  str(x) + " in this moment " + str(y) + " result in procent " + str(percents(x, y)))

#percents(79,114)
#print_percents()



#percents(78,108)
print_percents(78,108)
